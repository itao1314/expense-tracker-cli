# Milestone: v2 Improvements

## Task 1 — Export CSV
Add command:
expense export expenses.csv

Requirements:
- Export all records to CSV
- Columns: id, amount, description, category, date

## Task 2 — Delete expense
Add command:
expense delete <id>

## Task 3 — Edit expense
Add command:
expense edit <id> --amount 100 --description dinner

## Task 4 — Category summary
Update "expense report" to show totals by category.

## Task 5 — Package executable
Provide instructions to build a single-file executable using PyInstaller.